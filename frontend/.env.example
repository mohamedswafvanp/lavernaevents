# LavernaEvents Frontend Environment Configuration
# Copy this file to `.env` and adjust the values as needed.

# Base URL of the Django REST API (no trailing slash).
VITE_API_BASE_URL=http://localhost:8000/api